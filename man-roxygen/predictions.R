#' @param predictions A data frame of predictions made by sdmTMB using the
#' predict function based on the `newdata` prediction grid passed to the
#` predict funtion. Contains the predicted fixed effects, random effects,
#' spatial, and spatio-temporal based on the fit model estimates.